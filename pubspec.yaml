name: pam_p6_2026_ifs23019
description: "Menampilkan berbagai daftar dessert."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4
  launcher_name: ^1.0.2

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/dessert.png"

launcher_name:
  default: "Delcom Desserts Flutter"
  id: "Delcom Dessert Flutter"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
